# Docker Compose içindeki diğer konteynerlerin varsayılan guest ile AMQP bağlantısı
# (yalnızca geliştirme / ders ortamı). Production'da güçlü kullanıcı ve guest kapatma kullanın.

loopback_users.guest = false
